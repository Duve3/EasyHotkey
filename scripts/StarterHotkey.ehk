%WINAPI% FALSE  # disable use of the WINAPI in order to use pyautogui (makes it simpler when debugging)

%key% w PRESS  # press down the key "w"

%pause% 1s  # you can use anything from 1ms to 1h anything less or more and its unlikely to function properly. Also EHK only supports ms, s, m, and h for abbreviations and if none are used then seconds are assumed.
%key% w PRESS
%key% w RELEASE  # release the key "w"

# structure = %instruction% -> data for instruction